TÀI LIỆU PHÂN TÍCH HỆ THỐNG SAAS TELEGRAM BOTDự án: Hệ thống cung cấp Bot bán hàng tự động đa người dùng.1. Công nghệ Sử dụng (Tech Stack)Thành phầnCông nghệLý do chọnBackendNode.js + TypeScriptXử lý bất đồng bộ cực tốt cho Bot, TypeSafe giúp giảm 90% lỗi logic.FrameworkExpress.jsNhẹ, linh hoạt, dễ học và triển khai nhanh hơn NestJS.DatabaseMySQL (v8.0)Quản lý giao dịch đơn hàng chính xác, phổ biến, dễ backup/restore.ORMPrismaTự động tạo bảng từ code, hỗ trợ gợi ý code (Intellisense) cực mạnh.FrontendReact.js + TailwindCSSGiao diện Admin mượt mà, chuyên nghiệp, dễ tùy biến.Bot LibraryGrammYThư viện Bot mới nhất, tốc độ cao và tiêu tốn ít RAM hơn Telegraf.2. Cấu trúc Database (Schema)Sử dụng mô hình Multi-tenant (Nhiều khách hàng chung 1 database, phân biệt qua user_id và bot_id).A. Bảng User (Khách thuê của bạn)id: Int (PK, Auto Increment)email: String (Unique) - Dùng để đăng nhập.password: String - Mật khẩu mã hóa (bcrypt).plan: Enum (BASIC, PRO) - Để phân quyền số lượng bot được tạo.createdAt: DateTime.B. Bảng BotConfig (Cấu hình mỗi con Bot)id: Int (PK)userId: Int (FK -> User)botToken: String (Unique) - Token từ BotFather.botUsername: String - Tên định danh của Bot.bankName: String - Tên ngân hàng (ví dụ: MB Bank).bankAccount: String - Số tài khoản.bankOwner: String - Tên chủ tài khoản.isActive: Boolean - Trạng thái bot (để bạn khóa bot nếu khách hết hạn).C. Bảng Product (Sản phẩm của mỗi khách)id: Int (PK)botId: Int (FK -> BotConfig)name: String - Tên sản phẩm/dịch vụ.price: Float - Giá bán.description: Text - Mô tả sản phẩm.D. Bảng Order (Lịch sử mua hàng)id: Int (PK)botId: Int (FK -> BotConfig)customerTeleId: String - ID của người mua trên Telegram.totalAmount: Float.status: Enum (PENDING, PAID, CANCELLED).paymentCode: String - Mã chuyển khoản để đối soát tự động.3. Luồng hoạt động (Workflow)Luồng 1: Thiết lập Hệ thống (SaaS Flow)Admin (Bạn): Tạo tài khoản cho khách hàng thông qua DB hoặc trang Super Admin.Khách hàng: Đăng nhập vào trang React Admin -> Nhập Bot Token.Hệ thống:Gọi API Telegram: setWebhook(url: https://api.yourdomain.com/webhook/token_cua_khach).Lưu cấu hình ngân hàng và sản phẩm vào Database.Luồng 2: Webhook Xử lý tập trung (Core Logic)Đây là phần quan trọng nhất để tiết kiệm tài nguyên VPS:Sự kiện: Khách của khách (End-user) nhắn tin cho Bot.Telegram: Gửi dữ liệu Update về Endpoint duy nhất trên Backend.Backend Logic:TypeScript// 1. Nhận request từ /webhook/:token
// 2. Query Database tìm Bot tương ứng với Token này.
// 3. Nếu Bot tồn tại và isActive = true:
// - Nếu khách gõ /start -> Lấy 'welcome_text' từ DB phản hồi.
// - Nếu khách xem menu -> Lấy danh sách 'products' của botId đó hiển thị kèm nút "Mua".
// - Nếu khách bấm Mua -> Tạo 1 'Order' mới -> Trả về QR Ngân hàng + PaymentCode. 4. Phương án Treo VPS (Deployment)Để "treo" lên VPS ổn định nhất, bạn hãy làm theo các bước:Cài đặt môi trường: Cài Docker và Nginx Proxy Manager.Chạy Database: Dùng Docker để chạy MySQL (giúp cách ly dữ liệu và dễ nâng cấp).Chạy Backend:Sử dụng PM2 để chạy file JS sau khi đã biên dịch từ TS.Lệnh chạy: pm2 start dist/main.js --name "tele-saas-api".Cài đặt SSL: Sử dụng Nginx để trỏ domain (ví dụ: api.huongmmo.com) về cổng của Node.js (ví dụ: 3000) và bật HTTPS (bắt buộc để Telegram gửi Webhook).5. Ưu điểm của phương án nàyChi phí thấp: Chỉ cần 1 VPS 2GB RAM có thể chạy được hàng trăm Bot vì tất cả dùng chung 1 Process Node.js.Mở rộng dễ dàng: Sau này bạn muốn làm thêm tính năng (ví dụ: thông báo đơn hàng qua Telegram cho chủ shop), bạn chỉ cần sửa code tại 1 nơi duy nhất.Chuyên nghiệp: Khách hàng có Dashboard riêng để quản lý, không cần can thiệp vào code.Bạn có muốn mình tạo mẫu một file schema.prisma chuẩn cho các bảng trên không? Đây sẽ là nền móng để bạn khởi tạo Database chỉ trong 1 câu lệnh.
